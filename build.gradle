plugins {
    id "com.github.kt3k.coveralls" version "2.6.3"
    id "jacoco"
}

apply plugin: 'java'

jar {
    manifest {
        attributes 'Main-Class': 'build.classes.main.ttt.Main'
    }
}

<<<<<<< HEAD
task(run, dependsOn: 'classes', type: JavaExec) {
    main = "ttt.Main"
    classpath = sourceSets.main.runtimeClasspath
=======
task (run, dependsOn: 'classes', type: JavaExec) {
  main = "ttt.Main"
  classpath = sourceSets.main.runtimeClasspath
>>>>>>> adffe21fe51c8ba684df19dd7c362106b0e435f2
}

repositories {
    jcenter()
    mavenCentral()
}

dependencies {
<<<<<<< HEAD
    testCompile 'junit:junit:4.12'
=======
  // The production code uses the SLF4J logging API at compile time
  // Declare the dependency for your favourite ttt framework you want to use in your tests.
  // TestNG is also supported by the Gradle Test task. Just change the
  // testCompile dependency to testCompile 'org.testng:testng:6.8.1' and add
  // 'ttt.useTestNG()' to your build script.
  testCompile 'junit:junit:4.12'
>>>>>>> adffe21fe51c8ba684df19dd7c362106b0e435f2
    testCompile 'pl.pragmatists:JUnitParams:1.0.4'
}

jacocoTestReport {
    reports {
        xml.enabled = true
        html.enabled = true
    }


    afterEvaluate {
        classDirectories = files(classDirectories.files.collect {
            fileTree(dir: it,
                    exclude: ['**/Main.*',
                              '**/ConsoleDisplay.*',
                              '**/Minimax.*',
                              '**/ConsoleInput.*'
                    ])
        })
    }
}

run {
    standardInput = System.in
}
